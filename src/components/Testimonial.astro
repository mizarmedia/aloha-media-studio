---
// Single featured testimonial with results strip
const stats = [
  { value: '20-30k', label: 'Views/Month' },
  { value: 'All', label: 'Platforms' },
  { value: '2+', label: 'Years Together' }
]
---

<section class="py-24 bg-[var(--color-void)]">
  <div class="max-w-4xl mx-auto px-6">
    <!-- Results Strip -->
    <div class="flex flex-wrap justify-center gap-8 md:gap-16 mb-16">
      {stats.map((stat) => (
        <div class="text-center">
          <div class="text-3xl md:text-4xl font-bold text-[var(--color-coral)]">{stat.value}</div>
          <div class="text-sm text-[var(--color-stone)] mt-1">{stat.label}</div>
        </div>
      ))}
    </div>

    <div class="text-center">
      <!-- Quote icon -->
      <svg class="w-12 h-12 mx-auto mb-8 text-[var(--color-coral)]/30" fill="currentColor" viewBox="0 0 24 24">
        <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z"/>
      </svg>

      <!-- Quote -->
      <blockquote class="text-xl md:text-2xl lg:text-3xl text-[var(--color-cream)] font-medium leading-relaxed mb-10">
        "It has been a pleasure to work with Nation Goetter and his team at Aloha Media for the last two years. It is great that Nation makes the process of video and podcasting easy, it is even better that he is an expert. The quality of the product is higher than many other well-funded media enterprises."
      </blockquote>

      <!-- Author -->
      <div class="flex items-center justify-center gap-4">
        <img
          src="/images/testimonials/roger-hall.jpg"
          alt="Dr. Roger Hall"
          class="w-16 h-16 rounded-full object-cover object-top border-2 border-[var(--color-coral)]/30"
        />
        <div class="text-left">
          <div class="font-semibold text-[var(--color-cream)]">Dr. Roger Hall</div>
          <div class="text-sm text-[var(--color-stone)]">Host, Second Thoughts Podcast</div>
        </div>
      </div>
    </div>
  </div>
</section>
