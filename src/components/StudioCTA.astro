---
// Studio CTA - Book the space
// Research: Embedded booking works best for transactional purchases
---

<section id="studio" class="py-24 md:py-32 bg-[var(--color-charcoal)] relative overflow-hidden">
  <!-- Background image -->
  <div class="absolute inset-0">
    <img 
      src="/images/studio/studio-exterior-1.jpg" 
      alt="Aloha Media Studio"
      class="w-full h-full object-cover opacity-30"
      loading="lazy"
    />
    <div class="absolute inset-0 bg-gradient-to-r from-[var(--color-charcoal)] via-[var(--color-charcoal)]/90 to-[var(--color-charcoal)]/70"></div>
  </div>
  
  <div class="relative max-w-6xl mx-auto px-6">
    <div class="grid md:grid-cols-2 gap-12 items-center">
      <!-- Content -->
      <div>
        <p class="text-[var(--color-coral)] text-sm font-medium tracking-widest uppercase mb-4">
          The Studio
        </p>
        <h2 class="text-4xl md:text-5xl font-bold text-[var(--color-cream)] mb-6">
          Your creative space awaits
        </h2>
        <p class="text-[var(--color-mist)] text-lg mb-8">
          A fully-equipped production studio in the heart of Eagle, Idaho. 
          Perfect for video shoots, podcast recording, and photo sessions.
        </p>
        
        <!-- Pricing -->
        <div class="flex flex-wrap gap-4 mb-8">
          <div class="px-4 py-3 bg-[var(--color-void)]/50 rounded-lg border border-[var(--color-graphite)]">
            <div class="text-2xl font-bold text-[var(--color-cream)]">$100</div>
            <div class="text-sm text-[var(--color-stone)]">per hour</div>
          </div>
          <div class="px-4 py-3 bg-[var(--color-void)]/50 rounded-lg border border-[var(--color-graphite)]">
            <div class="text-2xl font-bold text-[var(--color-cream)]">$350</div>
            <div class="text-sm text-[var(--color-stone)]">half day (4hrs)</div>
          </div>
          <div class="px-4 py-3 bg-[var(--color-coral)]/10 rounded-lg border border-[var(--color-coral)]/30">
            <div class="text-2xl font-bold text-[var(--color-coral)]">$600</div>
            <div class="text-sm text-[var(--color-stone)]">full day (8hrs)</div>
          </div>
        </div>
        
        <!-- CTA -->
        <a 
          href="/studio"
          class="inline-flex items-center gap-3 px-6 py-3 bg-[var(--color-coral)] text-[var(--color-void)] font-semibold rounded-full transition-all duration-300 hover:bg-[var(--color-coral-light)] hover:scale-105"
        >
          Book Your Studio Time
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
          </svg>
        </a>
      </div>
      
      <!-- Features -->
      <div class="grid grid-cols-2 gap-4">
        {[
          { icon: "camera", text: "Sony cameras & Aputure lighting" },
          { icon: "mic", text: "Professional audio equipment" },
          { icon: "layout", text: "Podcast room + main studio" },
          { icon: "wifi", text: "High-speed WiFi" },
          { icon: "coffee", text: "Guest seating & refreshments" },
          { icon: "car", text: "Free parking" }
        ].map((feature) => (
          <div class="flex items-center gap-3 p-3 rounded-lg bg-[var(--color-void)]/30">
            <div class="w-8 h-8 rounded-full bg-[var(--color-coral)]/10 flex items-center justify-center">
              <svg class="w-4 h-4 text-[var(--color-coral)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
              </svg>
            </div>
            <span class="text-sm text-[var(--color-mist)]">{feature.text}</span>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
